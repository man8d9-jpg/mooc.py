learned: f strings are used in input functions too


value = []
num = int(input('How many items: '))
for i in range(1, num+1):
    item = int(input(f'Item {i}: '))
    value.append(item)
print(value)
