def line(length, character):
    if character == '':
        text = length * '*'
    else:
        text = length * character[0]
    return text

def triangle(size):
    for i in range(1, size + 1):
        print(line(i, '#'))

triangle(5)
