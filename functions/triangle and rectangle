def line(length, character):
    if character == '':
        text = length * '*'
    else:
        text = length * character[0]
    return text

def shape(size,T_char, height, R_char ):
    for i in range(1, size+1):
        print(line(i, T_char ))

    for i in range(height):
        print(line(size, R_char))

shape(4, '*', 5, '#')
